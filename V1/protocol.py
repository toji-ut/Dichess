# later on I want this to be converted to binary and then decoded from binary

def encode_move(move):
    # encode the move as a string
    return str(move)


def decode_move(encoded_move):
    return encoded_move
